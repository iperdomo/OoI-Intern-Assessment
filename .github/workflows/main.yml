name: main
on: [push]
env:
  CI_COMMIT: ${{ github.sha }}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      # SSH debugging
      #- uses: mxschmitt/action-tmate@v3
      - name: Build and test
        run: ./ci/build.sh
